model Usuario {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  nome      String?
  senha     String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
