{
  "name": "trabalhofinaltcs2",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo do Trabalho Final TCS — frontend React, backend NestJS e mobile React Native",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:all": "concurrently -n backend,frontend,mobile -c blue,green,magenta \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:mobile\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run start:dev --prefix backend",
    "dev:mobile": "npm start --prefix mobile",
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix mobile"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}

